{
  "name": "adder-wasm",
  "description": "Simple codebase for adding two numbers via WebAssembly with jco",
  "type": "module",
  "scripts": {
    "gen:types": "jco guest-types -o generated/types",
    "build:ts": "rollup -c rollup.config.mjs",
    "build:component": "jco componentize dist/adder.js --wit wit/world.wit --world-name adder --out adder.wasm --disable all",
    "build": "npm run gen:types && npm run build:ts && npm run build:component"
  },
  "dependencies": {
    "@bytecodealliance/jco": "^1.12.0",
    "@bytecodealliance/componentize-js": "^0.18.4"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.1",
    "@rollup/plugin-typescript": "^12.1.4",
    "rollup": "^4.48.0",
    "tslib": "^2.8.1"
  }
}
